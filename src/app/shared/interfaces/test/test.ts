import { Lookup, SharedProperties } from "../shared/shared";

export interface TestDto extends Lookup, Partial<SharedProperties> {

}
export interface AddTestDto extends Lookup, Partial<SharedProperties> {

}
export interface UpdateTestDto extends Lookup, Partial<SharedProperties> {

}
